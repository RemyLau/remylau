[user]
	name = Remy
	email = liurenmi@msu.edu
[push]
	default = simple
[color]
	ui = auto
[core]
	editor = nvim
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
